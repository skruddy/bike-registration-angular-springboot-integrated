This is a working and integrated Spring boot-Angular project.

Just close it on a local system, install sqlite, angular, npm.

Start the Spring boot project and run angular project via npm start and we are good to go.

hit the browser with 'localhost:4200/' or 'localhost:4200/admin' to get user interface.